const modulo1 = require('./modulo1')
console.log(modulo1)

//npm install left-pad
const leftPad = require('left-pad')
console.log(leftPad('Rodrigo', 10))

//quando criar um novo projeto, criamos o nosso arquivo package.json que armazena todas as dependências de forma prática para consulta

/*ao digitar no terminal npm start, executa o comando pois no package.json usamos um script com o nome de start*/

//devDependencies só na máquina de produção
